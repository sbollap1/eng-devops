parameters:
- name: serviceConnection
  type: string

steps:

  - checkout: eng-devops
    persistCredentials: true
    git: $(serviceConnection)
  
  - script: pwd ; ls -ltr

  - script: cd $(Build.SourcesDirectory)/ ; ls -ltr ;
    displayName: Step2 template Displaying from Template
