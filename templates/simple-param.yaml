parameters:
- name: yesNo
  type: boolean
  default: false
- name: svcconn
  type: string
- name: endpoint
  type: string
  
steps:
- checkout: ${{ parameters.endpoint }}
- checkout: self
- script: echo ${{ parameters.yesNo }}
